#include<string>
using namespace std;

extern int count;//As these two variables are global variables, they have to be declared as extern variables in the header file and as global variable in any one of the source files.
extern string subString;
extern int length;
bool parForHello(char c);//Declaration of the functions related to the main source file.
